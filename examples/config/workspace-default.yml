# =============================================================================
# linkora Workspace-Local Configuration
# =============================================================================
# This is a workspace-local config file
# Location: <root>/workspace/<workspace_name>/linkora.yml
#
# IMPORTANT: Workspace-local config can ONLY override the `sources` field.
# Other fields will be ignored with a warning.
# =============================================================================

# Override sources for this workspace
sources:
  local:
    enabled: true
    papers_dir: papers
    paths: []  # No additional paths for default workspace
